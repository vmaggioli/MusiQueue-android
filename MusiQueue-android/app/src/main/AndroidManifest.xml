<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.vince.youtubeplayertest">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name=".Activities.helper_classes.MyApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/MusiQueue"
        android:supportsRtl="true">
        <activity android:name=".Activities.starting_activities.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <!--
 ATTENTION: This was auto-generated to add Google Play services to your project for
     App Indexing.  See https://g.co/AppIndexing/AndroidStudio for more information.
        -->
        <meta-data
            android:name="com.google.android.gms.version"
            android:value="@integer/google_play_services_version" />

        <activity
            android:name=".Activities.hub_admin_only.QueueActivity"
            android:theme="@style/MusiQueue"
            android:label="@string/title_activity_queue"/>
        <activity android:name=".Activities.starting_activities.GettingStarted"
            android:theme="@style/MusiQueue"/>
    </application>

</manifest>